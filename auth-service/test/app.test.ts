// import request from 'supertest';
// import { app } from './setup';

// describe('Application Tests', () => {
//   it('should respond with 200 status for health check endpoint', async () => {
//     // This test assumes you have a health check endpoint
//     // You may need to modify this based on your actual API endpoints
//     const response = await request(app).get('/');
//     expect(response.status).toBe(200);
//   });

//   it('should have proper headers', async () => {
//     const response = await request(app).get('/');
//     expect(response.headers['content-type']).toContain('application/json');
//   });

//   // Add more test cases as needed
// });
